pipeline {
    agent any

    stages {

        stage('Build') {
            agent {
                docker {
                    image 'node:18-alpine'
                    reuseNode true
                }    
            }
            steps {
                sh '''
                    ls -la
                    node --version
                    npm --version
                    npm ci
                    npm run build
                    ls -al
                '''
            }
        } 
        stage('Debug') {
            steps {
                sh 'ls -la'  // List files to verify 'build/' exists
            }
        }
        stage('Build Docker images') {
            steps {
                sh '''
                    docker build -f ci/Dockerfile-playwright -t my-playwright .
                    docker build -f ci/Dockerfile-aws-cli -t my-aws-cli .
                '''
            }
        }
    }
}